#!/usr/bin/env node

var hljs = require('../node_modules/highlight.js'),
  fs = require('fs'),
  inf = process.argv[2] || 'infile needed',
  outf = process.argv[3] || 'outfile needed';

var code = fs.readFileSync(inf, 'utf8');
fs.writeFileSync(outf, hljs.highlightAuto(code).value);
